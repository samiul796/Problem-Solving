#include <bits/stdc++.h>
using namespace std;

int main()
{

    int number;

    cin >> number;
    int arr[number, number]; 
    for (int i = 0; i < number; i++)
    {
        for (int ii = 0; ii < number; ii++)
        {

            if (i == ii)
            {
                cout << 1;
            }
            else if ()
            {
            }
        }

        /* code */
    }

    return 0;
}

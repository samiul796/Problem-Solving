#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n;
    cin >> n;

    for (int i = 0; i < n; i++)
    {

        string word1, word2, array1[5] = {"pedra", "papel", "tesoura", "lagarto", "Spock"};
        cin >> word1 >> word2;
        for (int i = 0; i < n; i++)
        {
            /* code */
            if (word1 == array1[i])
            {
            }
        }
    }

    return 0;
}
